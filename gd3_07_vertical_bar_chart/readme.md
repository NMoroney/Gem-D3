
## Gem D3 : Vertical Bar Chart

Create a d3.js vertical bar chart for input TSV file of top ten US cities by area.

<img src="2606-gd3_vertical_bar_plot-01.jpg" width=600px>

Initial prompt :

```
Write a Javascript function to generate a d3.js vertical bar chart plot for the input data file us_cities-areas_km2.tsv.
The function should take input parameters of input data file name, plot width and plot height.
Generate the JavaScript code and save to file. 
Also generate an index.html file to call this javascript function. 
Do not start any processes to install or invoke an http server.
```

The above failed for some reason so I **nudged** the prompt:

```
Write a basic Javascript function to generate a d3.js vertical bar plot for the input data file us_cities-areas_km2.tsv.
The function should take input parameters of input data file name, plot width and plot height.
Generate the JavaScript code and save to file vertical_bar_plot.js.
Also generate an index.html file to call this javascript function. 
Do not start any processes to install or invoke an http server.
```
